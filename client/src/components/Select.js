import styled from "styled-components";

export default styled.select`
    background: #005827;
    color: #ffe8c4;
    font-family: 'Montserrat';
    font-size: 1rem;
    font-weight: 500;
    align-self: center;
    margin: .6rem;
    &:focus {
        outline: none;
    }
`
